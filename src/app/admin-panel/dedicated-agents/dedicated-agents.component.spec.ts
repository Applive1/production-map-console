/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { DedicatedAgentsComponent } from './dedicated-agents.component';

describe('Component: DedicatedAgents', () => {
  it('should create an instance', () => {
    let component = new DedicatedAgentsComponent();
    expect(component).toBeTruthy();
  });
});
