
import ServerControl from './server-control';

export default ServerControl.extend({
    tagName: 'aws-server',
    name: 'AWS Server',
    icon: '/assets/controls/aws.png',
    comment: 'an amazon server'
});