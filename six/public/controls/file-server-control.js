
import ServerControl from './server-control';

export default ServerControl.extend({
    tagName: 'file-server',
    name: 'File Server',
    icon: '/assets/controls/FileServer.png',
    comment: 'a file server'
});