
import ServerControl from './server-control';

export default ServerControl.extend({
    tagName: 'build-server',
    name: 'Build Server',
    icon: '/assets/controls/Builder.png',
    comment: 'a build server'
});