
import ServerControl from './server-control';

export default ServerControl.extend({
    tagName: 'mail-server',
    name: 'Mail Server',
    icon: '/assets/controls/Mail.png',
    comment: 'a mail server'
});